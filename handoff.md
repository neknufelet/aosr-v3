# Handoff

給下一個對話。先讀這份，再讀 `DIRECTION.md`。這份分三層：**要老闆回的**（給人看，一題四格）、**座標**（給下一個對話看）、**備查**（不用動）。待辦不列，全在 GitHub issue（見 `docs/decisions/backlog-in-github-issues.md`）。

## 要老闆回的（一題）

票 #368（標籤 decision）：品質登記簿正式數字的第一題，音色的目標傾斜。
- **發生什麼事**：登記簿現在填佔位的 0（水平）。上一代寫 −0.7 dB／八度、註 Harman，回原始來源查（票 #358）**找不到這個數**；ITU-R BS.1116-3 與 EBU 3276 兩份標準的房內頻響都是以平坦為中心的 ±3 dB 容許帶，不是傾斜目標。真的參考房報表量到 +1.46（往上），原因沒驗。
- **結果**：這一格不拍，傾斜那個分項永遠是測試基線，排名結果一直標「未校準」。
- **我建議**：A——目標＝0，出處標「標準原文」；這是唯一一年後查得回來的數。B——沿用 −0.7，出處只能標「老闆的產品選擇」。
- **你回什麼**：A 或 B（或別的數與理由）。容許帶、峰谷界線、平滑寬度是後面幾題。

## 座標（給下一個對話）

現在在哪（2026-09-19，計分段第一條鏈整條落地：物理結果→評估→代價→資格→排名第一次跑得通；第九段報表與治理層盤點已落地）：
- 2026-09-09 清空重來，規矩從 `v2-audit/` 重新長；卡、票、考卷的數字全部看狀態頁（機器現算），這裡不抄。
- 引擎走到：鏡像法一到五段加第十段、第七段有限元素 v3（gmsh 網格＋scikit-fem P2＋pydiso 雙精度）在主線；第八段晚期混響 v3（每面 6×6 格、雙精度直接解）在主線；第九段（#280 已關）在主線：決策紙 #288、頻率軸與有限元素 300 Hz／1/24 八度 94 點 #289、交接權重 #290、幾何細軸帶相位 #291、晚期衰減 T20（契約 2^-20，依據改正紙）與 T30（性質界線 2^-30）#292、三路接合報表＋命令列 #300。報表命令列 `uv run python -m aosr.physics.three_lane_report_cli <輸入.json> [--points]`（輸入格式在模組 docstring）；參考房 flat 一份完整報表單緒約 306 秒。
- 第七段兩份擋合併的考卷：剛性九點對解析解（用掉 2^-10 的 1.04%）、v3 對凍結 FEniCS 答案（`blueprint/fem_fenics_*.json`，最大 5.6e-14，界線 2^-30）；答案檔出身由卡 `fenics-answers-carry-provenance` 守。命令列 `uv run python -m aosr.physics.fem_rigid`（加 `--compare blueprint/fem_fenics_answers.json` 對答案）。
- 第八段判第一類（#274），上一代答案照擋每頻帶 2·1e-4，最大用掉 49.4%（lowabs）；吸收率用雙精度，跟上一代單精度吸收率差 3–4 個最小刻度是預期，不准拿格數當要求。命令列 `uv run python -m aosr.physics.late_energy_cli blueprint/reference_art_<材料>.json --compare`。
- **治理層盤點（2026-09-15 深夜到 09-16，票 #312 #313 #315 #296 #297）已落地，動手前要知道的五件**：①精度契約的尺（門檻數字）只住 `blueprint/precision_contracts.toml`，產品程式不准再寫一份、改值要帶新紙、每條一支變異考卷；②聲速、空氣密度這類基礎物理量只住 `src/aosr/config/data/physics_constants.toml`；③能力表 `src/aosr/config/data/capabilities.toml` 記每個入口驗過哪些房型、材料形式、頻率範圍、輸出欄，前端與之後的助理只照它開選項（這個用法寫在票 #315 上），表上標 validated（驗過）的那幾條要指到真的考卷；④被取代的決策紙要在同一支合併請求搬到 `docs/archive/`；⑤每支合併請求內文要一行「驗收：通過 <完整 head 提交 id>」，跟上主線後 head 變了要重驗。
- **客戶資料三句底線（#314，現在就生效，機器偵測等第一個客戶專案才立卡）**：私有資料只住本機工作目錄或私有 repo，不進這個公開 repo；公開 repo 只放參考房與合成資料；收據、票、決策紙不准帶客戶名、地址、量測原檔。
- **老闆不打指令**：命令列是給助理印答案核對用的，老闆要看數字就問助理、助理跑完貼表；之後老闆要的是能操作的 GUI 前端（不一定是網頁），等第九段報表有了再開題（#278 回 A）。
- 雲端 `verify` 2026-09-19 當天幾跑落在七分鐘到十二分半之間（考卷變多了，舊的「四五分鐘」已不準）；#140 已拿掉 conftest 預跑（全套只跑一次）；本機驗證指令見 `CLAUDE.md`。
- **工作樹（worktree，同一個 repo 的第二個工作目錄）**：2026-09-19 拍板（#364）一張票一個資料夾 `~/aosr-v3-work/<票號>-<短名>/`，樹住底下的 `tree/`、分支同名；開、查、拆只走 `uv run python -m governance.worktrees`（規矩在 `CLAUDE.md` 座標節與決策紙 `docs/decisions/worktrees-live-with-the-work-folder.md`），票合了就跑 `remove`。**現在只留一棵**：`~/aosr-v3-work/134-materials-loaders/`（PR #173 草稿，別刪；09-19 從舊位置搬過來，內容沒動）。它是 09-12 轉向前「照上一代子套件順序搬」的材料層一塊：頻率網格（freq_axis）、實驗設定格式（experiment_schema）、YAML 材料載入器，含 107 個對上一代的案例；雲端紅在三個插值浮點比對，原因未定案，要動先實跑；票 #134／#135 暫停但開著，不准硬關。**等功能真的需要時再重判**：#218 頻率軸拉成設定、第十段分格材料都會碰到頻率網格與材料載入，那時先問老闆接著修還是照需要重寫。同一天本機舊分支清過一輪（只留主線與這一條；PR #168 已關，它要解的事已不存在），每條被刪的分支指到哪一顆都記在 `~/aosr-v3-work/364-worktree-home/` 的證據夾，要救回來照那份做。

每一條標動詞，看了就知道要不要動：
- **計分段（2026-09-19 第一條鏈落地：#356 #345 #357 #359 都由合進主線的 PR 關了）**：品質登記簿 `src/aosr/config/data/quality_targets.toml`（目標、較差參考、門檻、權重的唯一住處；**第一版全部 baseline＝測試基線、數字是佔位**，升 calibrated 要帶結構化出處）＋載入器與內容指紋；評估器到排名層的凍結契約 `src/aosr/scoring/contract.py`（三個明確狀態：只量／有代價／不可估，變更走 PR）；音色評估器 `src/aosr/scoring/timbre.py`（只量不打分，能力表標 experimental）；排名層 `src/aosr/scoring/ranking.py`（三型代價、五種候選狀態、缺類不補值、指紋不同分表、任一基線設定整份標未校準、五塊排名資料）；整合考卷 `tests/engine/test_scoring_integration.py`（考卷裡有限元素那一路是假能量，守的是交接不是物理；峰谷界線用測試用登記簿放寬，理由在票 #359）。架構與規則在兩張決策紙與子票留言（#345 #348～#354），**開工前先讀子票**。**下一步**：正式數字一次一題（第一題 #368），查證表在工作區 `~/aosr-v3-work/358-thresholds/`（摘要在票 #358：−0.7 與 1/6 八度查不到出處、早期反射標準頻段是 1–8 kHz、殘響容許帶上一代抄的有三處與原圖不同）；接著 #349 多接收點（入口已在主線）→ #348 殘響（它的輸出要帶頻帶清單，排名層那三條資料資格規則才接得上，接法寫在排名層檔頭）。真的參考房報表量過一次（票 #359 留言）：最深的谷 75 Hz −12.8 dB、峰約 +5.6 dB、傾斜 +1.46，照佔位的 3 dB 界線連參考房都會被淘汰——峰谷界線要對著小房間低頻的真實量級拍。名稱：「第十段」是分格材料與多接收點，計分叫「計分段」。#338 仍開著。
- **低頻拖尾第 0 步量完（票 #352 留言；方法不變，只是成本證據）**：正式網格 P2 未知數 72,407、線性化後加倍；一個設定找出 300 Hz 以下全部模態單緒約 500 秒、尖峰約 6 GB；剛性房對解析解一對一全配上；**要以波數當特徵值**（以角頻率當特徵值時分解被擾動三萬多個主元、結果不可信），第 1 步鎖複數慣例時一起寫進決策紙；腳本、九跑原始檔、報告在工作區 `~/aosr-v3-work/352-modal-cost/`。每個候選都跑還是只跑前幾名，等候選時間預算拍了才決定。
- **記下沒卡會紅的洞（有票，不急）**：#318 型別警衛暫存目錄不可用時離開碼 1 而不是 2；#322 受管答案檔同一支合併請求改名成非 .json 會脫管；#332 出身那兩張卡只掃 `blueprint` 第一層，子目錄裡的答案檔不受管；#333 重匯格式檔的入口仍可被程式洗掉版控裡那兩份；#326 雲端設定檔同一步重複鍵、或一步既沒有 run 也沒有 uses（兩種寫法都是「這一步要做什麼」），沒有卡會紅。
- **分工（#245，2026-09-16 改；09-18 補回報索引）**：**一百行上下的改動由主對話自己寫**；工人只用在大改動或要平行跑的工作（派之前先問「這比我自己寫快嗎」）。#364 那支工具連考卷近千行、主對話自己寫了，老闆當場點出來——這種大小要派工人。找碴那一層仍然獨立、但收成一輪，只看真缺陷。派工單固定兩條：每一條需求回報對到「檔案 :: 符號＋考卷 :: 測試函式」（行號只當輔助，會漂）；沒做的要寫沒做。**修正後一定再送驗收，驗的就是要合的那顆 head**。2026-09-19 踩到的四件：接手的工人會把全套測試丟背景就結束、不交回報（驗收一律主對話自己跑，長指令用 setsid 才不會跟著 session 死）；兩個對話同時跑全套會互搶 CPU 到三倍慢，開跑前先看另一邊；找碴席的沙箱有時全唯讀、突變一個都跑不了，它的靜態判讀要主對話自己植入突變驗；驗收行的順序是**先提交、拿到新 head、驗過、改好合併請求內文，最後才推**（先推後改的話，推觸發的那一跑讀到舊的驗收行會紅，是規矩在做事；新開的合併請求就在建立時把驗收行帶進去），改內文用 `gh api -X PATCH`（`gh pr edit` 在這台會失敗，有時還不報錯；09-19 那次印的是一句 GitHub 舊版 Projects 的錯誤、內文沒改成——不管用哪一種，改完都要回讀內文確認）；等合併的期間主線往前走了，要用一顆 merge 提交跟上、再驗一次新 head。
- **記下的坑**：引擎考卷在預設平行 `-n auto` 下 numpy 多緒互搶，報表考卷單跑約 17 秒、平行時單題 45 秒以上；工人沙箱只寫自己的工作目錄（指示別叫它寫 `~/aosr-v3-work/`），有的工人連 /tmp 都寫不了、要它寫 /dev/shm；工人逾時被砍時工作常常已經在樹上，先看樹再決定要不要重派；別叫工人自己寫暫存腳本做「新舊逐位比對」，那種活它會做到逾時；程式裡寫上一代出處用模組點記法（`lib/...` 斜線路徑會被 refs 卡咬，別加放行名單）；全套測試跑到一半不准在同一棵樹提交或改檔（收尾那個「跑前跑後 repo 要一樣」的守衛會報錯，那一跑就作廢）；要停自己開的測試用行程編號停，別用 `pkill -f` 加路徑樣式（樣式會連自己那個 shell 一起對到）；`~/ghq` 與 `~/aosr-v3-work` 在兩顆磁碟，`git worktree move` 跨不過去，要搬就在新位置用同一條分支重開、再拆舊的；Claude Code 背景指令會被「低記憶體」誤判砍掉（它看 MemFree 不看 MemAvailable，官方 issue 92228／92448 未修），長計算用 setsid nohup＋結束標記檔＋監看。
- **報表還要記的事**：只收頻率無關實數阻抗（#309，能力表上已標 unsupported 不支援）；吸音很少的房（例如六面 α≈0.02）T20／T30 標算不出（#299）；報表驗收兩條非必修建議未做（模組 docstring 一句、原因字串可加逐點最低 dB）；有限元素與幾何在交接段的能量對齊沒有機器在守的檢查，只量過一次（見「交接段量過一次」）。
- **要開票問老闆（一次一題）**：①晚期混響契約寫死雙精度，換成第三階段 GPU 單精度（開新紙取代）；②段序紙排入材料最佳化階段（repo 裡的「第三段」是高階反射，別撞名）；③有限元素網格要不要加密：v3 正式網格自我收斂 250 Hz 帶 flat 0.07 dB、lowabs 0.13 dB（實驗室舊網格 0.06／0.04），精度對時間的取捨，等評分與時間預算有眉目再問。
- **交接段量過一次（2026-09-15，只有參考房一個接收點、flat 與 lowabs 兩組，不是考卷）**：150～300 Hz 逐點，有限元素對幾何平均 flat +0.45 dB、lowabs −0.07 dB；總能量相鄰兩點最大跳 flat 1.3 dB、lowabs 3.5 dB，不比 300 Hz 以上只有幾何時的 2.8～4.3 dB 大，看不到換手跳一階；lowabs 單點深谷（207 Hz 低 10 dB）是模態谷。**要查**：flat 交接段一路上去有限元素對幾何從 −4.7 dB 變 +2.8 dB，上端權重已近 0 所以總和看不出，但兩邊在 300 Hz 仍差 2.8 dB，原因未查。輸入與逐點輸出在 `~/aosr-v3-work/stage9-crossover-alignment/`；要不要立檢查等 #303 後再問老闆。
- **暫緩**：#269 沒改動就不重考（等第十段再加有限元素考卷、雲端時間吃緊時拿實測數字拍）；#218 頻率軸露到前端設定（產生器與 1/24 八度正式軸已在 #289 做完，只剩前端那一半）。
- **排著的量測（repo 外、還沒派工）**：#253 有吸音時有限元素 P2 在 GPU 單精度夠不夠，材料最佳化進 repo 前量完。
- **先別動**：#249（材料最佳化上 GPU 前要改的六格規矩），等材料最佳化真的開工；GPU 考卷要自架執行機，repo 是公開的，觸發權限要先拍。
- **老闆還沒決定、會卡後面的**：評分的數字（門檻、目標、權重——量法已拍，數字要回原文查再拍）；考卷 3（I-Simpa、物理性質）用不用；材料連續阻抗還是型錄；每組候選的時間預算（300 Hz 正式網格單緒每頻點約 3.2 秒、一份完整報表約 5 分鐘）；GUI 前端（第九段報表有了，可以開題）；晚期混響要不要比 6×6 更細；凹形房間是否不做。
- **自動跑的約定（2026-09-13 凌晨拍，仍有效）**：照段序做，只在五種情況停：要開新卡或改卡的邊界／門檻／白名單；某段契約要拿證據訂容差；雲端 verify 同一原因紅兩次；工人與找碴矛盾而驗不出誰對；要動 #173／#134／#135。
- **沒有退回機制**：CPU 只用 pydiso、GPU 用什麼就是什麼；出錯就報錯看原文，不准自動換解法；GPU 不能用時改 CPU 要人決定。
- **量測證據（repo 外）**：`~/aosr-v3-work/` 底下 `261-*`（第七段各段證據、FEniCS 答案產生腳本與代跑佇列在 `261-fenics-answers/`，映像備份在 `/home/florian/ghq/.backups/`）、`fenics1/`、`255-regular-mesh/`、`247-papers/`、`precision-remeasure/`、`compute-strategy/`、`fem-assembly-lab/`、`cudss-lab/`、`spineax-lab/`、`pydiso-ci-lab/`；GPU 套件版本與每次量測數字記在 `~/aosr-v3-work/` 那本版本帳本，每次測都追加。
- **機器驗證的缺口（重判後再立候選票）**：輸入檔與答案檔的正式 schema（報表那一半 #316 做完了，答案檔那一半還沒有）；物理性質的獨立真值晚期混響那一路做了（PR #323 的互易、整牆與同阻抗分格），幾何路在散射係數為 0 時有獨立真值了（#303 續量：算到收斂的非同調鏡像法，不含晚期混響），**大於 0 那一半仍然沒有**（分流參考跟定義共用同一組逐階能量，是同義反覆）；時紅時綠偵測。
- 共用零件：載入器 `governance/loader.py`、離開碼與輸出層 `governance/exit_codes.py`、後設測試 `tests/test_fixture_runner.py`、主線卡名單 `governance/mainline_cards.py`、CI `.github/workflows/verify.yml`；考卷分兩籃，治理層 `tests/`、引擎 `tests/engine/`。
- 主線 ruleset（合併門檻的設定，id `22615925`）：不准刪、不准改寫歷史、只能走 PR、`verify` 綠且跟上主線才准合。狀態頁由 `governance/status/` 推到機器分支 `status`、掛 GitHub Pages：https://neknufelet.github.io/aosr-v3/

## 備查

- 立卡：一張卡一個 PR，帶卡的 TOML、檢查程式、必紅樣本目錄（含控制樣本），後設測試八回合；加卡、或任何加了考卷的 PR，都要順手把 `green-must-be-real-green.toml` 受影響那一籃的 `collected_floor` 調到實跑收集數（#364 漏了，是找碴席提醒的）。雲端 `verify` 綠了才算。
- 清空前的備份在 `~/aosr-v3-blueprint-2026-09-09/`。
- 做法：分工試驗記在 #245（監督／施工／獨立驗收與停止規則；驗收席位模型全名 `deepseek-v4-flash`）。外部模型走 calling-other-models 並留收據，主對話逐條重跑、終審與合併；實作在 repo **外面**的獨立工作樹；派工指示寫成檔案、只給路徑；工人沙箱看不到 GPU、沒網路，GPU 與要上網的步驟由主對話在沙箱外跑；PR 內文用 `Closes #n` 關票；指示裡寫「不要派別的模型」（工人會自己去叫驗收）；工人用 `setsid nohup` 派、以 exit 檔＋Monitor 等（harness 背景任務會被檔案快取誤判低記憶體砍掉）；長測試在前景跑。
- 踩過的坑：大包資料寫成檔案只給路徑；數字沒實跑過標「未驗，推測」；工人會編出像真的代號，交叉引用對回原始檔；「有幾筆」跟「每一筆都被比」是兩件事；裁判自己要有控制組；對照組要驗它會紅；GPU 行程一出 ILLEGAL_ADDRESS 整個行程就毀，每個設定各開一個行程；結果檔每算完一列就寫；驗收也會讀錯（#254 第一輪三條有兩條是它誤讀），它說的每一條都回原文對。
- 政策：既有相容性合約維持；改公開呼叫契約算行為、不算結構，要改就當一張決定做。

## 這份怎麼寫（下一個對話照抄）

- 第一節永遠只有一題、四格（發生什麼事／結果／我建議／你回什麼），每格兩三句，最多兩個選項並說選哪個；沒題就寫「沒有」。那一題同時要有一張標籤 `decision` 的票，這裡只抄票號。
- 座標每一條標動詞；給機器看的代號只出現在座標與備查；數得出來的數字指去狀態頁；每個英文名詞同一句配中文；全篇日常講法。
- 已落地的事併成一行；超過 60 行就砍，舊事沉進決策紙；repo 外的路徑只寫到目錄、不寫帶副檔名的檔案（refs 卡會咬）；不寫 commit 與雲端 run 編號（identity 卡會咬）。
