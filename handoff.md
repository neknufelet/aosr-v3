# Handoff

給下一個對話。先讀這份，再讀 `DIRECTION.md`。這份分三層：**要老闆回的**（給人看，一題四格）、**座標**（給下一個對話看）、**備查**（不用動）。待辦不列，全在 GitHub issue（見 `docs/decisions/backlog-in-github-issues.md`）。

## 要老闆回的（一題）

**沒有。** 今天沒有標籤 `decision` 的開著票。上一題（長第一塊之前那三題）已經拍完，寫在 `docs/decisions/engine-first-block-config-shape.md`。

## 座標（給下一個對話）

現在在哪（2026-09-11 收工）：
- 2026-09-09 清空重來，規矩從 `v2-audit/` 重新長；決定與理由在 `docs/decisions/`。
- 主線 29 張卡（`governance/rules/`），藍圖 38 張全部有去向：由 `blueprint/remap_cards.py`（重算去向那支）算出，寫在 `cards-38.json`，不手抄。
- 共用零件：載入器 `governance/loader.py`、離開碼與輸出層 `governance/exit_codes.py`、後設測試 `tests/test_fixture_runner.py`、CI `.github/workflows/verify.yml`（一個 `verify` 工作跑全部檢查＋pytest＋ruff＋mypy）。
- 主線 ruleset（合併門檻的設定，id `22615925`）四條：不准刪、不准改寫歷史、只能走 PR、`verify` 沒綠不准合。
- 狀態頁由 `governance/status/` 推到機器分支 `status`、掛 GitHub Pages（靜態網頁託管）；入口檔 `CLAUDE.md`（規矩節由卡生成）與 `AGENTS.md`（指路牌）。

每一條標動詞，看了就知道要不要動：
- 要老闆回：沒有。
- 可以開工（不用老闆回）：#127 長第一塊 `config`——三題已拍，範圍是「自己家的 21 支」，另外 5 支各有票接手（#134 材料那一塊、#135 scoring 那一塊）。
- 排隊的工程題（不用老闆回）：#120 引擎測試的十五分鐘上限與那批「要靠機器以外狀態才判得出對錯」的考卷（部分已拍）、#136 擋 JAX 的考卷、#137 測試地板改成機器自己數、#138 量測程式進版控、#94 後設測試瓶頸、#59 delivery skill（一張票從開工到收工的流程機）。
- 去做完了、等老闆自己關：#123 鎖住 v2（把記號釘在最後一筆、倉庫設成唯讀）。
- 在等機器：暫緩 3 張卡，標籤 `deferred-cards`（一組一張票）——1 張等 v3 自己的設計報告、1 張等 repo 裡出現 shell 腳本、1 張等新家長出第一塊產品程式。
- 已拍板、已落地（今天）：第一塊的形狀（決策紙 `engine-first-block-config-shape`）——只做自己家的 21 支、不碰 JAX 的共用型別沉第 2 層、碰 JAX 的留第 3 層、考卷跟治理層同一個工作分開跑兩次。
- 已拍板、已落地（更早）：舊引擎不搬、新家一塊一塊重新長（決策紙 `engine-not-imported-new-home-grows-block-by-block.md`）；新家分層卡與寫法卡（一支檔 1000 行）；v2 鎖在記號 `v3-donor`。
- 只是看：狀態頁 https://neknufelet.github.io/aosr-v3/ ；要拍板的題永遠是標籤 `decision` 的開著票。

## 備查

- 立卡：一張卡一個 PR，帶卡的 TOML、檢查程式、必紅樣本目錄（含一份控制樣本）。後設測試對每張卡跑六回合（見 `tests/test_fixture_runner.py` 檔頭）。雲端 `verify` 綠了才算。立卡前先讀該卡在 `cards-38.json` 的規格。
- 加卡的 PR 要順手把 `green-must-be-real-green.toml` 的 `collected_floor` 調到那一跑的實跑收集數（不調，雲端會判「地板過期」）。
- 清空前的備份在 `~/aosr-v3-blueprint-2026-09-09/`（完整 git 歷史的 bundle 檔）。
- 做法：實作一律派子代理在獨立 worktree（同一個 repo 的另一棵工作樹）做，主對話只判題、驗收（雲端綠、看狀態頁）、合併；派工指示寫成檔案、只給路徑；PR 內文用 `Closes #n` 關票，不准手動關。立卡的 PR 多一道：派別家模型唯讀拿繞法去戳檢查程式。
- 踩過的坑：大包資料寫成檔案只給路徑；數字沒實跑過標「未驗，推測」；工人會編出很像真的代號，交叉引用都要對回原始檔；找碴那層要獨立而且要凶。今天多一筆：**票上的數字不帶指令，下一個人只能重跑或照抄**——一天抓到三個抄錯的數字。

## 這份怎麼寫（下一個對話照抄）

- 第一節永遠只有一題、四格（發生什麼事／結果／我建議／你回什麼），每格兩三句，最多兩個選項並說選哪個；沒題就寫「沒有」。
- 那一題同時要有一張標籤 `decision` 的票；這裡只抄票號，不抄內文。
- 座標那一節每一條都要標動詞。給機器看的代號只能出現在座標與備查。
- 每個英文工具名旁邊同一句要有中文說它在做什麼。不用沒驗過的數字。全篇用日常講法。
- 已落地的事併成一行、只留卡名或決策紙名、不留票號；連著兩行以上「已落地」就是帳本，該併。
- 超過 60 行就砍：舊事沉進決策紙，不留在這裡。
