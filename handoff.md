# Handoff

給下一個對話。先讀這份，再讀 `DIRECTION.md`。這份分三層：**要老闆回的**（給人看，一題四格）、**座標**（給下一個對話看）、**備查**（不用動）。待辦不列，全在 GitHub issue（見 `docs/decisions/backlog-in-github-issues.md`）。

## 要老闆回的（一題）

**沒有。** 上一題（載入器要不要有不傳路徑的預設）2026-09-12 拍完：**不加**，理由與代價在 `docs/decisions/config-loaders-keep-path-required.md`。

## 座標（給下一個對話）

現在在哪（2026-09-12 收工）：
- 2026-09-09 清空重來，規矩從 `v2-audit/` 重新長；決定與理由在 `docs/decisions/`。
- 主線 28 張卡（`governance/rules/`）；藍圖 38 張全部有去向（`blueprint/cards-38.json`，`remap_cards.py` 算的，不手抄）。
- **新家第一塊 `src/aosr/config` 已落地**（三刀 ＋ 一支總驗收修正：`ecd843d`→`525e914`→`4c72946`→`c91cbc9`）：20 支模組、9 個 `.toml` 逐位元搬進來、空的 `__init__.py`（v2 那個拉 JAX 的門面沒搬）、`paths.py`、`early_reflection.py`；逐條驗收度在票 #127 的留言。
- 共用零件：載入器 `governance/loader.py`、離開碼與輸出層 `governance/exit_codes.py`、後設測試 `tests/test_fixture_runner.py`、CI `.github/workflows/verify.yml`（一個 `verify` 工作跑全部檢查＋pytest＋ruff；mypy 由 `type-guard` 那張卡在 pytest 裡叫）；兩個籃子（綠卡 `[junit]`）治理層 295、引擎 `tests/engine/` 244。
- 主線 ruleset（合併門檻的設定，id `22615925`）四條：不准刪、不准改寫歷史、只能走 PR、`verify` 沒綠不准合。狀態頁由 `governance/status/` 推到機器分支 `status`、掛 GitHub Pages。
- 入口檔 `CLAUDE.md`（規矩節由卡生成）與 `AGENTS.md`（指路牌）。

每一條標動詞，看了就知道要不要動：
- 要老闆回：沒有。
- **下一個動作**：開第二塊——**#134 材料那一塊（第 3 層）**接手 `freq_axis`／`experiment_schema`／`material_loader`；**#135 scoring 那一塊（第 6 層）**接手 `fixed_anchor`／`scoring_v2_stage1`。兩張票的開工條件（#127 關掉）這一支 PR 合併就成立。
- 票分六批（GitHub 里程碑，用 `iss` 看）：**1 裁判補洞**（#155／#161／#162／#163，已合併 `5227889`）、**2 守門根治與立卡**（#148／#149／#16）、**3 第二塊**（#134／#135）、**4 順手**（#156／#157／#158／#166）、**5 帳與工具**（#151／#152／#138／#137／#140／#136）、**6 等／背景**（#120／#94／#59／#10／#15／#123）。
- 去做完了、等老闆自己關：#123 鎖住 v2（記號 `v3-donor` 釘在凍結點、倉庫唯讀）。
- 在等機器：暫緩 3 張卡，標籤 `deferred-cards`——其中「等新家長出第一塊產品程式」那一張的條件今天成立了，可以開工；另兩張還在等（v3 自己的設計報告、repo 裡出現 shell 腳本）。
- 已拍板、已落地：第一塊的形狀（決策紙 `engine-first-block-config-shape`——只做自己家的 21 支、不碰 JAX 的型別沉第 2 層、考卷一次跑兩個籃子）；舊引擎不搬、新家一塊一塊重新長（決策紙 `engine-not-imported-new-home-grows-block-by-block`，今天起與實作對得上）；新家分層卡與寫法卡（一支檔 1000 行）；載入器的路徑維持必填、不開方便預設的例外（決策紙 `config-loaders-keep-path-required`）。
- 只是看：狀態頁 https://neknufelet.github.io/aosr-v3/ ；要拍板的題永遠是標籤 `decision` 的開著票。

## 備查

- 立卡：一張卡一個 PR，帶卡的 TOML、檢查程式、必紅樣本目錄（含一份控制樣本）。後設測試對每張卡跑六回合（見 `tests/test_fixture_runner.py` 檔頭）。雲端 `verify` 綠了才算。立卡前先讀該卡在 `cards-38.json` 的規格。
- 加卡的 PR 要順手把 `green-must-be-real-green.toml` 裡**受影響那一籃**的 `collected_floor` 調到那一跑的實跑收集數（不調，雲端會判「地板過期」；籃子越小那個窗口越窄）。
- 清空前的備份在 `~/aosr-v3-blueprint-2026-09-09/`（完整 git 歷史的 bundle 檔）。
- 做法：實作一律派子代理在**獨立工作樹**（`git worktree add` 到 repo **外面**，別放 `.claude/` 底下免得髒了主樹）做，主對話只判題、驗收、合併；派工指示寫成檔案、只給路徑；PR 內文用 `Closes #n` 關票，不准手動關。找碴那一道：派別家模型**唯讀**、題目固定，它講的**一條一條自己重跑**才採信——找碴的人沒有 Bash，「它說的」永遠只是「去哪裡驗」。
- 踩過的坑：大包資料寫成檔案只給路徑；數字沒實跑過標「未驗，推測」；工人會編出很像真的代號，交叉引用都要對回原始檔。第一塊多兩筆：**「有幾筆」跟「每一筆都被拿去比」是兩件事**——答案檔的 case 集合相等可以全綠，單筆刪掉沒有人知道，要用逐筆刪的突變當證據；**裁判自己也要有控制組**——共用比對函式一鬆，幾百題一起假綠；**而且對照組本身要驗它會紅**——第 1 批的第一版「只差欄位」對照組是假的（拿裁判自己報的字串去造），舊裁判下一樣綠。
- 政策：合約是「數值與行為跟上一代一致」——**改公開呼叫契約**（把必填參數放寬成可選）算行為、不算結構；要改就當一張決定做。

## 這份怎麼寫（下一個對話照抄）

- 第一節永遠只有一題、四格（發生什麼事／結果／我建議／你回什麼），每格兩三句，最多兩個選項並說選哪個；沒題就寫「沒有」。
- 那一題同時要有一張標籤 `decision` 的票；這裡只抄票號，不抄內文。
- 座標那一節每一條都要標動詞。給機器看的代號只能出現在座標與備查。
- 每個英文工具名旁邊同一句要有中文說它在做什麼。不用沒驗過的數字。全篇用日常講法。
- 已落地的事併成一行、只留卡名或決策紙名、不留票號；連著兩行以上「已落地」就是帳本，該併。
- 超過 60 行就砍：舊事沉進決策紙，不留在這裡。
