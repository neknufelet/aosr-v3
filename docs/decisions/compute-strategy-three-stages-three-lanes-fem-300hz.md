---
title: 三個最佳化階段與三路計算的找法、裝置、精度和解析度
date_created: 2026-09-15
date_modified: 2026-09-22
status: accepted
kind: governance
supersedes: "compute-strategy-three-stages-three-lanes.md"
superseded_by: ""
summary: "房間形狀與喇叭座位用中央處理器搜尋，表面材料用圖形處理器梯度；有限元素上限改為 300 Hz，幾何路改用細頻率軸。沒有自動退回，圖形處理器規格進版本庫前另拍版本與考卷。"
---

# 三個最佳化階段與三路計算的找法、裝置、精度和解析度

## 問題

精度、裝置與算法散在多張契約紙和量測裡，沒有一處能看見三個最佳化階段與三路計算的全貌。
材料最佳化尚未排進段序，但它的規格已有量測可支撐；這張紙只定計算策略，不改段序或既有契約。
本紙取代前紙，同步更改有限元素的上限與取點，以及鏡像法和晚期混響的頻率解析度，並附成本實測與票號；其餘計算策略不變。

## 選項

- **A**：把票 #248 的兩張表全部定為規格；圖形處理器欄先列第三階段規格，進版本庫前另拍 JAX（自動微分計算庫）版本與圖形處理器考卷怎麼掛。
- **B**：只定前兩個階段，第三階段等梯度在實際迴圈跑過再定。

## 決定

採選項 A。表一與表二都是規格；任何解法出錯就報錯，程式不自動換解法，改用別的解法要由人另作決定。

**表一：每個最佳化階段怎麼算**

| 階段 | 固定 | 在調 | 找法 | 每試一次跑 | 裝置 | 精度 | 載入 JAX（自動微分計算庫） |
|---|---|---|---|---|---|---|---|
| ①房間形狀 | 無 | 房間比例 | Optuna（參數搜尋工具）搜尋 | 有限元素 | CPU（中央處理器），多行程、各單執行緒 | 雙精度 | 否 |
| ②喇叭與座位 | 房間 | 位置 | Optuna（參數搜尋工具）搜尋，不取梯度 | 鏡像法＋有限元素 | CPU（中央處理器），多行程、各單執行緒 | 雙精度 | 否 |
| ③表面材料 | 房間、位置、反射路徑 | 每面阻抗 | JAX（自動微分計算庫）梯度 | 有限元素＋鏡像法＋晚期混響 | GPU（圖形處理器）；還不能用時，由人決定改用 CPU（中央處理器）的 pydiso（稀疏直接解工具），程式不自動換 | 見表二 | 是 |

**表二：每一路怎麼算**

這張表不分矩形或多邊形，只支援凸形；凹形未排。

| 算路 | 演算法 | ①②用 | ③用 | 解析度 |
|---|---|---|---|---|
| 有限元素 | gmsh（產生網格的工具）四面體＋scikit-fem（有限元素組裝函式庫）組裝＋稀疏直接解 | pydiso-MKL（中央處理器的稀疏直接解工具），雙精度 | spineax（把 cuDSS 圖形處理器稀疏解法接進 JAX 自動微分計算庫的求解套件），P2（二次元素）雙精度；圖形處理器還不能用時，由人決定改用中央處理器的 pydiso，程式不自動換。雙精度是剛性牆（最難算準的情況）量的；有吸音時單精度夠不夠待票 #253 量，過了開新紙取代 | 元素與密度照數值工具決定；有限元素算到 300 Hz（含）；20～300 Hz 取 `f = 20·2^(n/24)`，共 94 點。#280 留言拍上限，#284 回 A 拍取點；#280 實測分解＋解一個頻點（單緒）／峰值記憶體為 250 Hz：1.7 秒／0.56 GiB，350 Hz：9.1 秒／1.48 GiB，500 Hz：60.4 秒／4.97 GiB（GiB 是 2^30 位元組） |
| 鏡像法 | 鏡像源；幾何的路徑、距離、到達時間與入射角用中央處理器雙精度，同一份路徑表產出頻率域與時間域 | numpy（陣列運算函式庫）雙精度 | JAX（自動微分計算庫）在 GPU（圖形處理器）用單精度 | 三階；20～300 Hz 為 1/24 八度，300 Hz 以上以同間距接續到 8000 Hz 八度帶上緣（原 4000，由 geometric-axis-ceiling-is-8khz-band-upper-edge.md 修正）；直達與反射帶相位疊加，報表頻帶取帶內能量平均。老闆在票 #285 回 A |
| 晚期混響 | 牆面分格能量交換，精確解 | 不用 | JAX（自動微分計算庫）在 GPU（圖形處理器）用單精度 | 每面 6×6 格；20～300 Hz 為 1/24 八度，300 Hz 以上以同間距接續到 8000 Hz 八度帶上緣（原 4000，同上修正）。老闆在票 #285 回 A |

有限元素與幾何路的硬切換以 ≤300 Hz 有限元素、>300 Hz 幾何路處理；「300 Hz 那一點歸有限元素」是助理補定，老闆未逐字拍板。頻率軸本身不另補 300 Hz 這一點：20 Hz 起的 1/24 八度點在 300 Hz 兩側是約 293.4 Hz 與 302.0 Hz，≤300 Hz 的 94 點由有限元素算、>300 Hz 的點只給幾何路；「300 Hz 那一點歸有限元素」指 300 Hz 這個邊界值落在有限元素那一側（助理補定）。票 #280 的成本欄是「分解＋解一個頻點（單緒）」；700 Hz 老闆說不用測。

主線 `src/aosr/config/fem_lane.py` 的 `FEM_FMAX_CAP_HZ = 250.0` 是照上一代搬來的設定；引擎籃（`tests/engine/`）的設定答案考卷 `tests/engine/test_shoebox_mesh.py` 拿它對上一代凍結值，本紙不改它。v3 正式路徑另立自己的 300 Hz 上限常數，名稱到實作時再定。
`docs/decisions/precision-contracts-geometry-energy-amplitude-fem.md` 的有限元素小節中，≤250 Hz、29 點是既有考卷的凍結題目；`docs/decisions/fem-contract-fenics-frozen-answers.md` 的 FEniCS（外部有限元素程式）題目檔自帶網格；`docs/decisions/fem-numerical-tools-scikit-fem-p2-pydiso.md` 所記 20～250 Hz 網格量測與既有 29 點成本證據也維持有效。
這些既有考卷與證據不因本紙失效；剛性九點考卷改用哪張網格到實作時再定，雲端時間會增加。這是票 #280 留言 2 記下的落地影響。

圖形處理器欄是第三階段規格；進 repo（版本庫）前要先拍 JAX（自動微分計算庫）版本與 GPU（圖形處理器）考卷怎麼掛，歸票 #249。

## 為什麼

- 前兩階段不取梯度，可用 CPU（中央處理器）的多行程搜尋；材料階段只跑一條最佳化，可讓 JAX（自動微分計算庫）把三路梯度一起放到 GPU（圖形處理器）。
- 梯度單精度圖形處理器對雙精度的量測，有限元素（稠密解）為 5.4e-4、鏡像法為 5.5e-5、晚期混響為 2.3e-7，全部沒有符號翻轉；各路的精度選擇有實測支撐。
- 有限元素的雙精度依據是剛性牆（最難算準的情況）；有吸音的真實房間可能可用單精度，但必須等票 #253 量過並以新紙取代，不能在這張紙先假定。
- 「圖形處理器還不能用時由人決定改用中央處理器」與沒有退回機制一致，避免程式在出錯時靜默換解法。

## 代價

- 有限元素契約中「對上一代 2^-12」那一層綁著上一代規則網格；改用 gmsh（產生網格的工具）後不再成立，要換 `docs/decisions/precision-contracts-geometry-energy-amplitude-fem.md`。
- 晚期混響契約寫死雙精度，而本紙第三階段定為單精度，要換 `docs/archive/precision-contract-art-late-energy-exact-solve.md`。
- 現有段序只把最佳化列為之後另排，要在 `docs/decisions/engine-stage-order-three-to-ten.md` 排入材料最佳化階段。
- 上述三張各自另開 `decision`（決策）票，照 `docs/decisions/legacy-answers-three-roles.md` 的規則開新紙取代；這張紙不改它們。
- 圖形處理器重跑不逐位元相同，雲端考卷只能用容差比較；版本與考卷掛法尚須票 #249 拍板，有吸音時的單精度尚須票 #253 實測。

## 拍板

2026-09-14，老闆在票 #248 回「A」，助理建議 A。票文與拍板紀錄在外部工作區 `~/aosr-v3-work/247-papers/inputs/`；量測與規則盤點在 `~/aosr-v3-work/compute-strategy/evidence/`、`~/aosr-v3-work/precision-remeasure/`、`~/aosr-v3-work/cudss-lab/evidence/`、`~/aosr-v3-work/fem-assembly-lab/evidence/` 與 `~/aosr-v3-work/pydiso-ci-lab/evidence/`。
