__pycache__/
*.pyc
.venv/
.pytest_cache/
/governance/receipts/
# ruff 的快取。ruff 自己會在裡面放一份忽略檔，所以列舉集合本來就看不到它；
# 明寫一行是為了不倚賴那個行為（規矩卡 style-guard 那半在 CI 上會跑 `uv run ruff check`）。
.ruff_cache/
# mypy 的快取。同一個理由（mypy 也會自己在裡面放一份忽略檔，不倚賴那個行為）。
# 規矩卡 type-guard 的檢查程式自己不會產生它——它把 --cache-dir 指到系統暫存目錄、
# 跑完刪掉，因為 cwd 是掃描根，而必紅樣本樹也是掃描根，快取目錄會變成版控樹裡的殘留。
# 明寫這一行是為了手上直接跑 `uv run mypy` 的時候也不會在根層長出一個沒人宣告的目錄
# （那會被 file-placement-allowlist 咬）。
.mypy_cache/
# 檢查程式跑動態探針時開的暫存目錄。它們一律叫 aosr-<用途>-<亂數>，開在被掃的那棵樹裡面
# （必須在掃描根裡，探針量的就是「那棵樹上跑起來會怎樣」），跑完自己刪掉——刪不掉就
# raise ToolBroken 讓那支檢查回 2，不會靜靜留著。今天有四種：check_exit_code_honest 的
# aosr-empty-probe- 與 aosr-shell-probe-、commit_author_allowlisted 的 aosr-range-、
# secrets_never_committed 的 aosr-secrets-。
#
# 為什麼要寫這一行（issue #44）：後設測試現在平行跑（pyproject.toml 的 `-n auto`），每個
# 工人（worker，平行跑測試的子程序）跑完都會量一次真 repo 的 git status（收尾守衛
# real_repo_left_untouched）；先跑完的那個工人量到的時候，別的工人的探針目錄還開在樹上——
# 那不是殘留，是別人手上正在用的東西。這些目錄有沒有被清乾淨，由開它的那支檢查自己
# fail closed 守著（清不掉就回 2），不靠這條收尾守衛。
aosr-*/
# 本機 agent 工具的施工現場（派工單、工人 log、收據、工作樹）。它不進版控、GitHub 上
# 看不到，但在此之前也沒有被忽略，所以主樹的 git status 永遠掛著十幾筆，VS Code 的
# Source Control 面板看起來像「有一堆沒提交的改動」——那不是改動，是還沒被丟掉的本機檔。
# 派工指示住在這裡、只給路徑，所以整層留著，只是不給 git 看。
.claude/
