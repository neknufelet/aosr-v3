# 樣本卡：只給 scan-scope-has-no-holes 的必紅樣本用，不是真的規矩。
# 這張卡上只有這支檢查會讀的那幾欄（scope／scope_kind／check）——它的 scope_kind 打錯字，不在列舉裡。
id = "sample-card"
human = "樣本卡：scope_kind 打錯字（不是 files 也不是 commits）。"
scope = ["docs"]
scope_kind = "commit"
check = "governance/checks/stub_docs_only_scanner.py"
