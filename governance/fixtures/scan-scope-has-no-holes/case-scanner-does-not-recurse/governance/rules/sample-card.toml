# 樣本卡：只給 scan-scope-has-no-holes 的必紅樣本用，不是真的規矩。
# 這張卡上只有這支檢查會讀的那幾欄（scope／scope_kind／check）——它宣告了整個 docs，實際只掃第一層。
id = "sample-card"
human = "樣本卡：宣告整個 docs，檢查只掃第一層、不遞迴。"
scope = ["docs"]
check = "governance/checks/stub_shallow_scanner.py"
