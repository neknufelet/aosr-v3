壞法：放行名單上的 `handoff.md` 超過行數上限（卡上登記 max_lines = 60，這一份 63 行）。
放行只免掉檔名那一條，不免行數。這一份守的是找碴席指出的最大縫——
「白名單直接豁免了違規物件本身」：handoff.md 跟 v2 的 docs/next.md 是同一種會長大的檔，
所以放行要帶上限，不然等於無條件豁免。上限 60 是 handoff.md 自己寫的（「這份超過 60 行就該砍」）。
