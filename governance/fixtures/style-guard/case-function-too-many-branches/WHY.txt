第④條的後半：函式分支數超過卡上登記的門檻。

一長串平行的 if 寫在同一支函式裡。行數刻意壓在門檻底下（一行一個 if），
所以只犯「分支數」那一半——證明這兩個門檻是兩顆各自會咬的牙，不是一顆。

分支數算的是函式身體裡 if／for／while／except／with／三元／推導式／布林運算／
assert／match case 的節點數，巢狀的 def 各自算、外層不把內層算進去。

這一份是完整的迷你掃描根：一支違規的 .py、一張只帶 id 與 [settings] 的迷你卡，加這份 WHY.txt。
