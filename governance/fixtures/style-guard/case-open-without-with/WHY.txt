第③條：開了檔不用 with。

`fh = open("data.txt")` 之後就算記得 close，中間任何一個例外都會讓那個檔案描述子留著。
`with` 是唯一保證關得掉的寫法。

路徑刻意寫成一個純字面值（沒有字串拼），所以第②條不會跟著咬——一份樣本只證一件事。

這一份是完整的迷你掃描根：一支違規的 .py、一張只帶 id 與 [settings] 的迷你卡，加這份 WHY.txt。
