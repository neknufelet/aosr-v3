第②條：字串拼出來的路徑直接當開檔的參數。

`"logs" + "/" + name` 這種寫法自己組分隔符，作業系統的路徑規則（重複斜線、
上一層、名字裡本來就有斜線）全部要自己想到，而且平台一換就得再想一次。
路徑要用 Path 接。

這一份刻意把開檔寫在 `with` 的頭上，所以第③條不會跟著咬——一份樣本只證一件事，
兩條同時咬就分不出是哪一條在守（v2 事故 guard-teeth-shadow-each-other 的形狀）。

這一份是完整的迷你掃描根：一支違規的 .py、一張只帶 id 與 [settings] 的迷你卡，加這份 WHY.txt。
