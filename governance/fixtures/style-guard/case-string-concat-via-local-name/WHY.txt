第②條的一手轉：先把拼好的字串指給一個名字，再拿那個名字當路徑參數。

`p = "docs" + "/" + name` 之後 `os.path.exists(p)`。這是零成本的繞法：
行為跟直接寫在參數位置一模一樣，只是中間多一個名字。判準因此走一手資料流
（同一個作用域裡的直接指定），不是只看參數位置那一格。

這一份是完整的迷你掃描根：一支違規的 .py、一張只帶 id 與 [settings] 的迷你卡，加這份 WHY.txt。
