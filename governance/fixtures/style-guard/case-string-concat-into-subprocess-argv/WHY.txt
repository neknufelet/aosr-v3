第②條：拼出來的路徑塞進子程序的 argv。

`["cat", "logs" + "/" + name]` 這一格是路徑，跟開檔那一格是同一件事——
子程序看到的還是那個自己組出來的字串。

刻意只看 argv 那一格（第一個位置參數，或關鍵字 args=）：input=／env= 那些格子
放的是內容與環境，不是路徑，看了會誤咬。

這一份是完整的迷你掃描根：一支違規的 .py、一張只帶 id 與 [settings] 的迷你卡，加這份 WHY.txt。
