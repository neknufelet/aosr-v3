必紅樣本：門檻的值剛好等於離開碼那三個數之一。

stub_depth_cap.py 寫的是 `MAX_DEPTH`，值落在離開碼白名單裡。光按值放行的話這種寫法整條
繞過去——而且不是假想的：主線上兩支檢查的遞迴深度上限就是這個形狀，那是真的能把整關
關掉的門檻。所以名字命中卡上登記的「名字像門檻」樣式時，值在白名單裡也一樣要具名登記。

這一份跟主線那兩個的差別只有一件事：主線那兩個在卡的 [[settings.allow]] 有登記並寫了理由，
這棵樹裡的例外清單是空的。合規的對照道具在同一棵樹裡，不准被咬。
