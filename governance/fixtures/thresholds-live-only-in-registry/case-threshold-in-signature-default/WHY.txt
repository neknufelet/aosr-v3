必紅樣本：門檻藏在函式簽章的預設值裡。

stub_signature_default.py 把上限寫成 `def check(scan_root, max_lines=200)`。簽章的預設值在
模組載入的當下就算好了，跟寫在模組層級是同一件事，只是名字換成參數名——找碴席點名這是
第一版設計繞得過去的洞（設計只咬 `NAME = 數字`，樣本卻列了這種寫法，自相矛盾），這一份
就是把那個洞釘起來。

界線也一起釘：函式**內部**的字面值不算門檻（那是演算法），所以這一份刻意只在簽章上放數字，
函式裡不放。合規的對照道具在同一棵樹裡，不准被咬。
