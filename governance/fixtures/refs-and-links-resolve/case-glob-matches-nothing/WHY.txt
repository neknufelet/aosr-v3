必紅樣本：glob 寫得出來，但一個檔都命中不到。

`notes/design-note.md` 寫了 `data/collapse-by-*.json`。glob 算解析得到——**前提是真的有東西
命中**；這棵樹的 data/ 底下只有一份 keep.txt，那個 glob 命中零個，等於死引用。
這一份證明 glob 不是無條件放行（不然「加個星號」就成了繞過去的寫法）。

樣本裡的死 glob 會被咬，那是目的。這一份單獨餵過檢查、確認它自己會回 1。
