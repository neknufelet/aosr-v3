必紅樣本：設定檔（toml）的欄位值指到不存在的檔。

`config/service.toml` 的 entry 指著 src/app/main.py，這棵樹裡沒有。掃描面不是只有 md——
卡登記的副檔名清單裡有 toml，設定檔裡的路徑一樣是引用，搬檔沒改設定就是死引用。

樣本裡的死引用會被咬，那是目的。這一份單獨餵過檢查、確認它自己會回 1。
只犯這一條：整棵樹沒有別的路徑形狀的字串。
