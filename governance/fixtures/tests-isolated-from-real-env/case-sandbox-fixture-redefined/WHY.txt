sandbox fixture 被第二次定義。「唯一」是這條規矩的地基：只要有第二支同名 fixture，
測試要到的就可能是那支假的（例如直接把真樹當 sandbox 交出去），而靜態判準看到的仍是
「有用 fixture」，於是綠燈掛在正在碰真樹的測試上——這正是 v2 的死法。
這一份是完整的迷你掃描根：卡 ＋ tests/conftest.py（真的那支）＋ 一支自己再定義一次的
測試檔 ＋ 這份 WHY.txt。
