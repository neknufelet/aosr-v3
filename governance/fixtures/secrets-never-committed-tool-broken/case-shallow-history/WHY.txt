該回 2 的樣本：淺 clone。

歷史宣告最後一行是 shallow，所以檢查建完那段歷史之後會再做一次 `--depth 1` 的本機 clone，
拿那棵淺樹來掃。淺樹裡看不到舊提交，「掃過全歷史」就是假話——這種時候正確答案是 2
（這一跑不算數），不是 0（乾淨），也不是 1（有違規）。

為什麼要一份這種樣本：CI 的 checkout 預設就是淺 clone。少了這一關，把 workflow 裡的
fetch-depth: 0 拿掉之後，這支檢查會在只看得到一個提交的樹上安靜回綠——那正是這個 repo
的頭號死因。本機不是淺 clone，所以這件事只證得起來在樣本上。
