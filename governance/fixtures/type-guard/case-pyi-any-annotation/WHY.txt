第②層：型別存根檔（.pyi）的標註位置出現 Any。

`parse(text: str) -> dict[str, Any]` 的回傳標註裡有一個 Any。第①層的 mypy（型別檢查器）
允許明寫 Any，所以整份是乾淨的；這棵樹回 1 的唯一理由必須是 type-guard（型別警衛）
第②層真的把 .pyi 納入並咬到那個標註。

這一份是完整的迷你掃描根：一支違規的 .pyi、一張迷你卡、一份迷你 [tool.mypy]，加這份 WHY.txt。
