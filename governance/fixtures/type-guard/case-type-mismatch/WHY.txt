第①層：型別對不上。

`add(left: int, right: int)` 標得好好的，`total()` 卻把字串 `"1"` 餵進第一格。
這是「標註寫了但對不上」那一種，跟缺標註是兩件事，所以另開一份樣本。

第②層在這一份刻意完全乾淨（沒有 Any、沒有 cast、沒有抑制註解）。

這一份是完整的迷你掃描根：一支違規的 .py、一張迷你卡、一份迷你 [tool.mypy]，加這份 WHY.txt。
