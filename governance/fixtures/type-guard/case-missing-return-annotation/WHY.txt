第①層：公開函式缺回傳標註。

`count_rows(rows: list[str])` 參數標了、回傳沒標。mypy 嚴格模式那一包含
disallow_untyped_defs，所以這一份在第①層回一筆違規。

第②層在這一份刻意完全乾淨（沒有 Any、沒有 cast、沒有抑制註解），
所以這一份證的就是「第①層自己咬得到」這一件事。

這一份是完整的迷你掃描根：一支違規的 .py、一張只帶 id 與 [settings] 的迷你卡、
一份只帶 [tool.mypy] 的迷你設定檔（第①層的尺），加這份 WHY.txt。
