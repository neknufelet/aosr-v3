控制樣本：已知會咬的最小輸入。一個 job，跑測試那一步寫了 continue-on-error: true。
刻意只犯第一條（有 timeout-minutes、有真的在跑測試的步驟、沒有 if、run 裡沒有吞離開碼的字樣，
apt 那一步用 `&&` 串起兩段，各自命中卡上登記的三個字，而且只有安全的 `-q`、`-y`、
`--no-install-recommends`，所以不增加 hits；測試那一步開頭就是抄寫員，第五條沒有別的違規，
沒有 PUSH_MAX_ATTEMPTS 所以第六條零對象），
所以它證明的是「最基本那條路徑還活著」——它回 0 就代表這支檢查死了。
