第五條：收據那個 job（verify）底下有一步 `uv run ruff check` 既沒包抄寫員，
第一個字 `uv` 也不在水管名單裡（名單登記的是兩個字的 `uv sync`）。
這就是 2026-09-10 之前主線的形狀：ruff 那一步刻意不包，理由只寫在 workflow 的
註解裡，收據上那一步只剩雲端記的紅綠，0／1／2 分不開。同一份裡另有兩步是對的
（`uv sync --locked` 是水管、pytest 那一步包了抄寫員），所以紅的只會是 ruff 那一行。
