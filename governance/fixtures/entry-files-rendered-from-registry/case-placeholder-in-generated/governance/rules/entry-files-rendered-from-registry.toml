# 樣本用的卡：只帶渲染規矩節要用到的三格（id、human、[mountpoint].blocks_merge）、check，
# 以及 [settings]。每一份樣本都是獨立的掃描根，檢查程式的門檻只從掃描根自己的
# governance/rules/ 讀，讀不到就回 2（不回 0）。
#
# 這一份的人話第一句裡刻意留了一個沒填完的佔位（TODO）。兩份入口檔的規矩節是真的用這張卡
# 重生出來的，所以「等於重生結果」那一條沒破、標記完整、指路牌也對——唯一的違規是
# 生成段裡留著佔位。這就是為什麼那顆牙不能靠「等於重生結果」代勞：佔位寫在卡上的時候，
# 重生出來的那一段本來就帶著它，逐字比對照樣回綠。
id = "entry-files-rendered-from-registry"
human = "樣本用的人話：入口檔的規矩節由登記簿生成，這一句還沒填完 TODO。"
check = "governance/checks/entry_files_rendered_from_registry.py"

[mountpoint]
blocks_merge = true

[settings]
entry_files = ["CLAUDE.md", "AGENTS.md"]
max_lines = 40
placeholder_markers = ["{{", "TODO", "TBD", "XXX", "<填>"]
# 指路牌那一份的固定內容，從主卡的形狀複製（樣本裡刻意寫短）。
pointer_text = '''
# 樣本的入口檔：真正的入口檔是 CLAUDE.md

樣本用的指路牌：開工先讀 CLAUDE.md，這一份不放內容。
'''
