只犯一條：下層 import 上層（模組層的真 import）。

config 在第二層、physics 在第三層，config 的檔頭直接 `from aosr.physics import solver`。
physics 這邊什麼都不引，所以不會順便變成環——這一份證明「往上引」那顆牙自己會咬。

v2 事故 core-layer-imports-orchestration-layer 的形狀就是這個：核心層反過來依賴上面那一層。
