所有條款同時犯的那一份。

一棵樹裡：下層引上層、相對 import 爬出套件根、同一層成環、非 runtime 碰 jax.config、
非 runtime 用 jax.experimental 翻精度、非 runtime 寫環境變數並提到那一族的名字、
三種動態取模組（importlib／裸 __import__／sys.modules），還有一個沒登記在層次表裡的模組。
餵下去回 1（跟每一份樣本一樣），報告行的 hits 會明顯大於 1。

為什麼還要這一份：前面每一條各有一支只犯那一條的樣本，那是「每顆牙自己會咬」的證據；
這一份是「幾顆牙湊在一起不會互相遮蔽」的證據——v2 事故 guard-teeth-shadow-each-other
的形狀就是前一個條件先回、後面那幾顆永遠沒機會咬。每一條的訊息都要出現在輸出裡。
