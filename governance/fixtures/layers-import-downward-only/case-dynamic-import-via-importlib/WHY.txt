只犯一條：不是 runtime 的模組用 `importlib.import_module` 動態取模組。

這一份是找碴席 2026-09-11 實測的 B2：字串是執行期才算出來的，靜態上看不出它引到哪一層，
所以「下層不准引上層」那一條完全繞得過去——這裡引的正是上面那一層。
第一版把這種寫法列成「已知的洞」，現在改成禁：新家不需要它。

而且刻意用改名 import（`import importlib as il`）：判準走名字解析，改名照樣咬。
