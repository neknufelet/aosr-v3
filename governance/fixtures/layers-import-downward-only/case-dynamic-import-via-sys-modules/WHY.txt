只犯一件事：不是最底下那一層的檔，從「已經載進來的東西」那張清單裡把模組撈出來。

這一份是 2026-09-11 找碴戳出來的：只要那一層已經被別人載進來過，
用一段文字當鑰匙就撈得到，連「去拿」那一行都不必寫，前面幾條一條都看不到。

這一份故意寫成最直白的一行，只會被抓到一處。
「改名字照樣咬」的證據在別的樣本上：`case-dynamic-import-via-importlib` 改了名字、
`case-non-runtime-touches-jax-config` 也改了名字、`case-non-runtime-writes-os-environ`
換了拿法——判準只有一套，三份一起證明改名繞不過去。
