只犯一條：不是 runtime 的模組寫環境變數。

而且是 `from os import environ` 那一種寫法——攤平後的字面裡沒有 `os.environ`，
只比字面的實作咬不到。名字解析追得到它回指 (os, environ)，所以照樣咬。

寫的那一格名字刻意不是 JAX_／XLA_ 開頭：這一份要證明的是「碰 os.environ」那一格自己會咬，
不是被環境變數名那一格順便咬到的。
