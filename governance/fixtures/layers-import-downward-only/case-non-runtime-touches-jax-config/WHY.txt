只犯一條：不是 runtime 的模組碰 JAX 的全域設定物件。

而且是用「改名 import」那一種寫法（`from jax import config as jc`）——
只比字面的實作在這一份上 hits=0。判準走名字解析（governance/names.py），
問的是「這個運算式回指哪一個 (模組, 名字)」，所以改名照樣咬。

v2 事故 import-time-global-flip-poisons-suite：模組載入的時候翻一次全域設定，
從此整套測試跑在另一組設定上，紅綠上只看得到「昨天會過今天不過」。
