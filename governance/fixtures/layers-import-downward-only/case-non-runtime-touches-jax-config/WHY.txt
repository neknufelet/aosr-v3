只犯一件事：不是最底下那一層的檔，去動 JAX（算數用的函式庫）的全域設定。

而且是用「改名字再拿」那種寫法（`from jax import config as jc`）——
只比對字面長相的做法，在這一份上什麼都抓不到。這支檢查是先把每個名字追回它真正的出處
再比對，所以改名字照樣咬。

上一代出過的事：有一支檔一被載入就偷改了一次全域設定，
整套測試從此跑在另一組設定上，而紅綠燈上只看得到「昨天會過、今天不過」。
