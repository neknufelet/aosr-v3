由 __file__ 推出來的自我定位，一樣是在程式裡自己決定模組搜尋路徑。這個形狀立卡當時放行過
（樹裡有 13 行照抄的寫法，一刀切會讓同時在立卡的 PR 在主線上變紅），issue #34 把那 13 行
全部拿掉，這一條就收成一律紅——這份樣本是那個收緊的證據：它餵下去回 0，就代表放行又回來了。

為什麼不需要那一行：走 uv run python -m（cwd 在 repo 根）本來就 import 得到；跑測試那邊要的
是設定檔裡宣告的 pythonpath，那是設定、走 PR 看得到、只有一份，不是每支程式各自插一行。

唯一的違規是這一條：鎖檔在、uv sync 帶了 --locked、命令位置沒有裸的直譯器與工具。
