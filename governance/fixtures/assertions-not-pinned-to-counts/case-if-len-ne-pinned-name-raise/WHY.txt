零成本繞法套在守門的 if 上：EXPECTED_DEFECTS = 3 再 if len(x) != EXPECTED_DEFECTS: raise。
跟直接在 if 裡寫 3 同一個病——數字仍寫死在同一支測試檔裡，行為一模一樣，換個名字寫躲不掉。
分界線仍是同一條：同一支檔裡指定的數字算寫死；import 進來的、對照別處登記的值算合規。
這一份證明「常數指派」這半在 if/raise 這顆新牙下也被咬。
這一份是完整的迷你掃描根：tests/ 底下一支違規的測試檔，加這份 WHY.txt。
