守門的 if 把數量鎖死：if len(findings) != 3: raise。
這是「assert len(x) == 3」換一件衣服：數量仍寫死在測試檔裡，多抓到一筆真缺陷就把閘弄紅，
守門的閘變成在懲罰改善。只看 ast.Assert 的檢查會漏掉它——病不在 assert 關鍵字，
在「數量對死一個寫死的整數」這件行為本身。if 的本體要有 raise 才咬：沒有 raise 的
if 只是一段條件分岔，不是守門，不算鎖死。
這一份是完整的迷你掃描根：tests/ 底下一支違規的測試檔，加這份 WHY.txt。
