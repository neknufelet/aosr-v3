零成本繞法：EXPECTED_DEFECTS = 3 再 assert len(x) == EXPECTED_DEFECTS。
找碴席在 cards-38.json 的 still_leaky_reason 點名的第一種繞法（n = 3 再比）。
數字仍然寫死在同一支測試檔裡，行為跟直接寫 3 一樣，所以照咬。
分界線在這裡：同一支檔裡指定的數字算寫死；import 進來的、對照別處登記的值算合規。
