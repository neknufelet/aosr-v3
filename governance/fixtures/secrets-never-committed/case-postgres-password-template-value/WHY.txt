v2 那件事故的形狀，這張卡的第一要求就是咬得到它。

事故原文（v2-audit/lessons.json 的 secrets-committed-and-never-scanned）：容器的資料庫密碼
是一個八位十六進位字串，跟一個公開兩年的樣板檔一模一樣；當時**已經有一支通用 secret-scan，
它對這個案例假陰性**。低熵、沒有前綴、不像金鑰——所以「看起來像金鑰嗎」這種判準看不到它。

這裡的值換成另一個八位十六進位字串（不是事故原文那個），形狀一樣。第②層咬它，靠的不是熵，
是「名字帶了 PASSWORD 這個字樣，而值不是佔位符」這個賦值語境。
