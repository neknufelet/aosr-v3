# 樣本用的歷史宣告。這棵樹不是 git 工作樹，檢查會照這份在暫存目錄建一段真的歷史再掃。
# 一行一筆，順序就是提交順序：
#   commit add    <路徑> <內容取到行尾>   那一筆新增（或改寫）這個檔
#   commit delete <路徑>                  那一筆刪掉這個檔（歷史裡那顆 blob 還在）
#   shallow                               建完之後多做一次 --depth 1 的本機 clone
# 這個檔本身不進內容掃描（它是檢查程式的道具，裡面的假金鑰是刻意的宣告）。
#
# 這一份的歷史刻意乾淨：這個樣本要證的事在現在這棵樹上，不在歷史裡。
commit add notes.txt 樣本歷史的第一筆，內容刻意乾淨
