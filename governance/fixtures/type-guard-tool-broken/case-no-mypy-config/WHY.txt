該回 2 的樣本：卡在、.py 在，但這棵樹裡沒有 mypy 的設定檔。

第①層的嚴格度只住在卡上登記的那個設定檔（`config_file`，今天是 pyproject.toml 的
[tool.mypy]）。設定檔不在，這支檢查就不知道自己拿的是哪一把尺——那時候「沒問題」
這句話不算數，回 2 不回 0。

刻意不給預設值：檢查程式自己帶一組嚴格度旗標的話，設定檔被人改寬了也看不出來，
而卡面會照樣宣稱「跑的是嚴格模式」。

裡面那支 sample_module.py 本身完全合規（兩層都咬不到），所以這棵樹回 2 的唯一理由
就是尺不在，不是它踩到什麼。
