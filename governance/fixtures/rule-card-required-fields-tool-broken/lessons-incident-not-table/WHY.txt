該回 2 的樣本：v2-audit/lessons.json 的最外層是表、incidents 是陣列，但其中一筆是字串。
清單裡有一筆不是表就取不出它的 id，那份清單就不能當成完整的權威——拿剩下幾筆去判
「對不到就是違規」會開始冤枉人。第三關回 2（工具自壞），這一跑不算數。
這一份證明「外層形狀對了、裡面有一筆壞掉」不會被靜靜跳過。對應
governance/checks/rule_card_required_fields.py:166 那條分支（某一筆事故不是一張表）。
