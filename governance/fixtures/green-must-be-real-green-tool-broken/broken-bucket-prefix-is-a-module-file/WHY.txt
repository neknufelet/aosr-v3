籃子前綴 tests.engine. 撞上同名的模組檔 tests/engine.py——分籃講不出唯一答案。

收據那邊拿 classname、原始碼那邊拿檔案路徑，前綴指到資料夾的時候兩邊必定同答案；
指到的是一支模組檔就不是了：tests.engine.TestThing 是「那支檔裡的類別」還是
「那個資料夾底下的模組」，classname 自己講不出來。判不出來就不判：離開碼 2。
猜大小寫（類別名通常大寫開頭）是在猜別人的命名習慣，不是判準。
